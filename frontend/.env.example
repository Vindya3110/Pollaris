# Frontend makes relative API calls (/api/...) which are proxied by nginx to the backend.
# No VITE_API_URL needed — the nginx config handles backend routing.
#
# If you want to run the frontend dev server directly against a backend
# (bypassing nginx), uncomment and set this:
# VITE_API_URL=http://localhost:8080
